# Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        UTM EANS Parser                           │
│                   (Reverse Engineered from                       │
│                    https://utm.eans.ee/)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
        ┌─────────────────────┴─────────────────────┐
        │                                            │
        ↓                                            ↓
┌──────────────────┐                      ┌──────────────────┐
│  Data Layer      │                      │  Display Layer   │
│  (fetchUAS.ts)   │                      │  (layers.ts)     │
└──────────────────┘                      └──────────────────┘
        │                                            │
        │ GeoJSON                                    │ MapLibre
        ↓                                            ↓
┌──────────────────────────────────────────────────────────┐
│              Processing Layer                             │
├──────────────────────────────────────────────────────────┤
│  • routeArea.ts    - Route generation with buffer        │
│  • intersections.ts - Spatial analysis                   │
│  • sorting.ts      - Priority ordering                   │
│  • colors.ts       - Visual styling                      │
└──────────────────────────────────────────────────────────┘
                              │
                              ↓
                      ┌───────────────┐
                      │  example.ts   │
                      │  (Integration)│
                      └───────────────┘
```

## Data Flow

```
User Action
    │
    ↓
┌─────────────────────────────────────────────┐
│ 1. Draw Route                                │
│    Points: [{lng, lat}, {lng, lat}, ...]    │
└─────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────┐
│ 2. createRouteArea()                         │
│    • Close polygon if needed                 │
│    • Create segments for line mode           │
│    • Apply buffer (METERS / 1000 → KM)      │
│    Output: Buffered FeatureCollection       │
└─────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────┐
│ 3. fetchUAS()                                │
│    GET /utm/uas.geojson                     │
│    Output: UAS zones GeoJSON                │
└─────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────┐
│ 4. getIntersectingZones()                    │
│    • Union all route segments                │
│    • Test each zone with booleanIntersects  │
│    Output: Array of conflicting zones       │
└─────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────┐
│ 5. sortUASFeatures()                         │
│    Pass 1: Sort by altitude                  │
│    Pass 2: Sort by restriction severity      │
│    Pass 3: Sort by feature type              │
│    Pass 4: Sort by operation state           │
│    Output: Prioritized conflicts             │
└─────────────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────────────┐
│ 6. Display Results                           │
│    • Apply colors from colors.ts             │
│    • Render with MapLibre layers             │
│    • Show conflict information               │
└─────────────────────────────────────────────┘
```

## Component Dependencies

```
example.ts
    ├── fetchUAS.ts
    │   └── (no dependencies)
    │
    ├── routeArea.ts
    │   └── @turf/turf
    │       ├── buffer
    │       ├── midpoint
    │       └── point
    │
    ├── intersections.ts
    │   ├── fetchUAS.ts (types)
    │   └── @turf/turf
    │       ├── booleanIntersects
    │       ├── booleanPointInPolygon
    │       ├── union
    │       ├── featureCollection
    │       └── feature
    │
    ├── sorting.ts
    │   └── fetchUAS.ts (types)
    │
    ├── colors.ts
    │   └── (no dependencies)
    │
    └── layers.ts
        └── colors.ts
```

## Critical Path: Buffer Conversion

```
User Input: 50 meters
    │
    ↓
┌─────────────────────────────────────────────┐
│ CRITICAL CONVERSION                          │
│                                              │
│ bufferMeters / 1000 = bufferKilometers      │
│ 50 / 1000 = 0.05                            │
│                                              │
│ ❌ WRONG: turf.buffer(geom, 50, {units: 'km'})   │
│    → Creates 50km buffer (100x too large!)  │
│                                              │
│ ✅ CORRECT: turf.buffer(geom, 0.05, {units: 'km'})│
│    → Creates 50m buffer (exact!)            │
└─────────────────────────────────────────────┘
```

## Sorting Priority Matrix

```
┌──────────────┬──────────┬──────────────┬─────────────┐
│   Feature    │ Altitude │ Restriction  │   State     │
├──────────────┼──────────┼──────────────┼─────────────┤
│ Priority 1   │ Lower ↑  │ PROHIBITED   │ ACTIVATED   │
│ Priority 2   │    ↓     │ REQ_AUTH     │ TAKEOFF_REQ │
│ Priority 3   │    ↓     │ CONDITIONAL  │ APPROVED    │
│ Priority 4   │ Higher ↓ │ NO_RESTRICT  │ PROPOSED    │
│ Priority 5   │          │              │ CLOSED      │
└──────────────┴──────────┴──────────────┴─────────────┘

Each column is a separate sort pass!
```

## Color Mapping

```
┌─────────────────────┬──────────────────────┬─────────────┐
│   Restriction       │   HSL Color          │   Visual    │
├─────────────────────┼──────────────────────┼─────────────┤
│ NO_RESTRICTION      │ hsl(120, 50%, 40%)   │ 🟢 Green    │
│ CONDITIONAL         │ hsl(90, 65%, 40%)    │ 🟡 Yellow   │
│ REQ_AUTHORISATION   │ hsl(60, 65%, 40%)    │ 🟠 Orange   │
│ PROHIBITED          │ hsl(0, 100%, 52.5%)  │ 🔴 Red      │
└─────────────────────┴──────────────────────┴─────────────┘

Opacity:
  • Low altitude (< 120m):  0.15
  • High altitude (≥ 120m): 0.05 (0.15 / 3)
```

## Error Handling Pattern

```
try {
    ┌─────────────────────────────────┐
    │ Turf.js Operation               │
    │ (booleanIntersects, union, etc) │
    └─────────────────────────────────┘
} catch (e) {
    ┌─────────────────────────────────┐
    │ console.error(e)                │
    │ return false / []               │
    └─────────────────────────────────┘
}

This pattern is used EVERYWHERE in the original!
```

## File Organization

```
parser_proto/
│
├── 📄 Documentation
│   ├── README.md              ← Start here
│   ├── SUMMARY.md             ← Overview
│   ├── API.md                 ← Function reference
│   ├── IMPLEMENTATION_NOTES.md ← Critical details
│   ├── TESTS.md               ← Test suite
│   └── ARCHITECTURE.md        ← This file
│
├── 🔧 Core Implementation
│   ├── fetchUAS.ts            ← Data fetching
│   ├── routeArea.ts           ← Route generation
│   ├── intersections.ts       ← Spatial analysis
│   ├── sorting.ts             ← Priority sorting
│   ├── colors.ts              ← Color scheme
│   └── layers.ts              ← MapLibre layers
│
├── 📦 Integration
│   ├── example.ts             ← Usage examples
│   └── index.ts               ← Main export
│
└── 🎯 Entry Points
    ├── index.ts               ← Import from here
    └── example.ts             ← See examples here
```

## Integration Points

```
Your Application
        │
        ↓
┌───────────────────────────────────┐
│  import { planFlight }            │
│  from './parser_proto'            │
└───────────────────────────────────┘
        │
        ↓
┌───────────────────────────────────┐
│  const result = await planFlight( │
│    route, mode, buffer            │
│  )                                │
└───────────────────────────────────┘
        │
        ↓
┌───────────────────────────────────┐
│  result.canFly                    │
│  result.needsAuthorization        │
│  result.conflicts                 │
└───────────────────────────────────┘
```

## Performance Characteristics

```
Operation              Time      Notes
─────────────────────────────────────────────────
fetchUAS()            ~300ms    Network dependent
createRouteArea()     ~10ms     CPU bound
getIntersectingZones() ~50ms    Depends on zone count
sortUASFeatures()     ~5ms      In-place sort
Total Workflow        ~400ms    Typical case
```

## Browser Compatibility

```
✅ Chrome/Edge 90+
✅ Firefox 88+
✅ Safari 14+
✅ Mobile browsers (iOS Safari, Chrome Mobile)

Requirements:
  • ES2020+ support
  • Fetch API
  • MapLibre GL JS compatible
```
